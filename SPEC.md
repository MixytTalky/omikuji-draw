# 日式手繪御神籤 — 設計規格書

**Slug**: `omikuji-draw`
**版本**: 1.0.0
**建立日期**: 2026-03-31
**狀態**: 規劃中

## 核心功能描述
帶有極簡日式質感的開運工具。使用者在畫布上隨手繪製線條注入意念，系統將亂數抽出大吉至大凶的專屬籤詩，並搭配一張和風極簡插畫，提供每日運勢指引。

## 目標關鍵字
1. 線上抽籤
2. 御神籤
3. 每日運勢
4. 大吉大凶
5. 日式占卜

## SEO
- Title: 日式手繪御神籤 - 畫出你的每日運勢 | Tool-Maker
- Description: 最具和風質感的線上御神籤！在畫布隨手畫出你的意念，隨機抽出大吉到大凶的專屬籤詩與日系插畫，為你指引每日運勢。免安裝，支援手機。
- 主要關鍵字: 線上抽籤、御神籤

## 廣告規劃
- UTM Campaign: `tool-omikuji-draw-v1.0.0`
- UTM Source: `omikuji-draw`
- 廣告模式: header / sidebar / footer

## 功能與互動流程規劃
#### 功能 1：手繪注入意念
- **輸入**：使用者在畫面上的 `<canvas>` 區域用手指或滑鼠隨意畫出圖形。
- **處理**：追蹤 touch/mouse 移動軌跡長度。當軌跡長度達到一定閾值，判定意念注入完成，亮起「求籤」按鈕。
- **輸出**：即時的墨跡或柔和線條筆觸，以及從 disable 變成可點擊狀態的按鈕。
- **邊界條件**：若無任何繪圖操作或長度過短，按鈕保持 disable；支援「清除重畫」。

#### 功能 2：演算與抽出命籤
- **輸入**：點擊「求籤」按鈕。
- **處理**：隱藏畫布，套用線性淡出入轉場。系統從內建的 7 種命籤（大吉、中吉、小吉、吉、末吉、凶、大凶）亂數抽取。機率可依需求調整。
- **輸出**：呈現該籤級對應的：1. 圓形裁切插畫、2. 籤級名稱、3. 籤詩文段。
- **邊界條件**：確保動畫過渡不超過 0.5s（遵循動效規範）。

#### 功能 3：重測與分享
- **輸入**：點擊結果頁下方的按鈕。
- **處理**：觸發原生 Web Share API 或回到畫布初始狀態。
- **輸出**：分享彈窗，或重置的空白畫布。

## 視覺風格
- **主題色系**：極簡底色 `#F9FAFB`，主要文字 `#1C1C1E`，點綴色（按鈕 Hover / 御神籤高光）為硃砂紅 `#E63946`。
- **字體**：標題與籤詩文案使用 Serif (襯線體)，UI 按鈕與介面使用 Sans-serif (無襯線體)。
- **禁 emoji 規則**：工具完全禁用原生 emoji，改以 Inline SVG 取代。
- **風格關鍵字**：Minimalist, Japanese Zen, Modern Laboratory.

## UI 草圖
```
[HEADER AD - 728x90]
┌──────────────────────────────────────┐ ┌──────────┐
│  (首頁 / 畫面置中)                     │ │          │
│  ⛩️ 放空思緒，在下方畫出你的意念          │ │ [SIDE    │
│  ┌───────────────────────────────┐   │ │  BAR     │
│  │                               │   │ │  AD]     │
│  │       ( Canvas 繪圖板 )         │   │ │          │
│  │                               │   │ │          │
│  └───────────────────────────────┘   │ │          │
│  [⭕ 重新整理]      [✨ 求取命籤](鎖)  │ │          │
└──────────────────────────────────────┘ └──────────┘
[FOOTER AD - 728x90]

(結果頁)
┌──────────────────────────────────────┐
│  (淡入呈現)                          │
│                                      │
│      (圓形裁切插圖 160x160)           │
│                                      │
│        【 大 吉 】                     │
│    "風平浪靜，此刻即是最好的安排"         │
│                                      │
│  [🔄 再抽一次]     [📤 分享運勢]       │
└──────────────────────────────────────┘
```

## 圖片需求清單 
- **OG Image** (1200x630): 日系玄幻動漫風（新海誠質感的絕美光影），主角在神社會意場景。
- **結果插畫** (共 7 張): 圓形裁切 `160x160`，採用「有主角系列」的日系玄幻動漫風格，分別描繪主角在大吉～大凶（如：迎著黃金旭日燦笑、在風雨中前行等）的情境。
- **按鈕圖示**: 清除、求籤、重抽、分享（使用 Inline SVG `currentColor`，禁用 emoji）。

## 技術備註
- 不使用原生 emoji，所有介面元素改寫為 Inline SVG。
- UI 將融入「Kiwimu 極簡現代實驗室風」：高灰度背景、大留白、襯線體（Serif）大標與籤詩，佐以細邊框按鈕。
- 手機端 Canvas 需處理 `touch-action: none` 防止頁面捲動拉扯。
